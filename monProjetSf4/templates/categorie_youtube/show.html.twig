{% extends 'base.html.twig' %}

{% block title %}CategorieYoutube{% endblock %}

{% block body %}
    <h1>CategorieYoutube</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie_youtube.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ categorie_youtube.title }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('categorie_youtube_index') }}">back to list</a>

    <a href="{{ path('categorie_youtube_edit', {'id': categorie_youtube.id}) }}">edit</a>

    {{ include('categorie_youtube/_delete_form.html.twig') }}
    {{ include('meteo.html.twig') }}
{% endblock %}
