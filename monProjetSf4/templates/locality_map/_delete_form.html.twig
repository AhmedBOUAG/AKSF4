<form method="post" action="{{ path('locality_map_delete', {'id': locality_map.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ locality_map.id) }}">
    <button class="btn">Delete</button>
</form>
