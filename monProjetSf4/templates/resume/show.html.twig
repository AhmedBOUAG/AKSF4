{% extends 'base.html.twig' %}

{% block title %}Resume{% endblock %}

{% block body %}
    <h1>Resume</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ resume.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ resume.titre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ resume.description }}</td>
            </tr>
            <tr>
                <th>Limites</th>
                <td>{{ resume.limites ? resume.limites|json_encode : '' }}</td>
            </tr>
{#            <tr>
                <th>Points</th>
                <td>{{ resume.points ? resume.points|json_encode : '' }}</td>
            </tr>#}
            <tr>
                <th>DateAd</th>
                <td>{{ resume.dateAd ? resume.dateAd|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('resume_index') }}">back to list</a>

    <a href="{{ path('resume_edit', {'id': resume.id}) }}">edit</a>

    {{ include('resume/_delete_form.html.twig') }}
{% endblock %}
