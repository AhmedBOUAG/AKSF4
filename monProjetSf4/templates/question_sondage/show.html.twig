{% extends 'base.html.twig' %}

{% block title %}QuestionSondage{% endblock %}

{% block body %}
    <h1>QuestionSondage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ question_sondage.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ question_sondage.titre }}</td>
            </tr>
            <tr>
                <th>Approval</th>
                <td>{{ question_sondage.approval ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>DateCreation</th>
                <td>{{ question_sondage.dateCreation ? question_sondage.dateCreation|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('question_sondage_index') }}">back to list</a>

    <a href="{{ path('question_sondage_edit', {'id': question_sondage.id}) }}">edit</a>

    {{ include('question_sondage/_delete_form.html.twig') }}
{% endblock %}
